<mvc:View
    controllerName="project_d.controller.InvoiceList"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:mvc="sap.ui.core.mvc"
>
    <List
        id="invoiceList"
        class="sapUiResponsiveMargin"
        width="auto"
        items="{
            path: 'invoice>/Invoices',
            sorter: [
                {
                    path: 'ShipperName',
                    group: true
                },
                {
                    path: 'ProductName'
                }
            ]
        }"
    >
        <headerToolbar>
            <Toolbar>
                <Title text="{i18n>invoiceListTitle}"/>
                <ToolbarSpacer/>
                <SearchField
                    width="50%"
                    search=".onFilterInvoices"
                />
            </Toolbar>
        </headerToolbar>
        <items>
            <ObjectListItem
                core:require="{
					Currency: 'sap/ui/model/type/Currency'
				}"
                title="{invoice>Quantity} x {invoice>ProductName}"
                number="{
					parts: [
						'invoice>ExtendedPrice',
						'view>/currency'
					],
					type: 'Currency',
					formatOptions: {
						showMeasure: false
					}
				}"
                numberUnit="{view>/currency}"
                numberState="{= ${invoice>ExtendedPrice} > 50 ? (${invoice>ExtendedPrice} >= 1000 ? 'Success' : (${invoice>ExtendedPrice} >= 500 ? 'Warning' : 'Error')) : ${invoice>ExtendedPrice} > 9 ? 'Warning' : ${invoice>ExtendedPrice} > 8 ? 'Success' : ${invoice>ExtendedPrice} &lt; 3 ?  'None' : 'Information' }"
            >
                <firstStatus>
                    <ObjectStatus
                        core:require="{
							CurrencyStatus: 'project_d/model/CurrencyStatus'
						}"
                        text="{
							path: 'invoice>Status',
							formatter: 'CurrencyStatus.statusText.bind($controller)'
						}"
                    />
                </firstStatus>
            </ObjectListItem>
        </items>
    </List>
</mvc:View>
